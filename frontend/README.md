# Tennis Technique Analyzer – Frontend

React app optimized for mobile (Android) use.

## Quick start

```bash
cd frontend
npm install
npm start
```

By default it expects the backend at `http://localhost:8000`.
On Android, change the API base URL to your Mac's IP on the Wi‑Fi network.
